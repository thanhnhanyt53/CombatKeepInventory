<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.votri</groupId>
    <artifactId>CombatKeepInventory</artifactId>
    <version>1.1.0-SNAPSHOT-build1</version>
    <packaging>jar</packaging>

    <name>CombatKeepInventory</name>
    <description>
        Combat-aware inventory and death handling plugin for Bukkit/Paper-compatible servers.
    </description>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <repositories>

        <!-- Paper API -->
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>

        <!-- WorldGuard / WorldEdit -->
        <repository>
            <id>enginehub</id>
            <url>https://maven.enginehub.org/repo/</url>
        </repository>

        <!-- PvPManager -->
        <repository>
            <id>nitrado</id>
            <url>https://repo.nitrado.net/repository/maven-releases/</url>
        </repository>

    </repositories>

    <dependencies>

        <!-- ================================================== -->
        <!-- Bukkit/Paper API                                  -->
        <!-- ================================================== -->

        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21.11-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>


        <!-- ================================================== -->
        <!-- WorldEdit API                                     -->
        <!-- Optional integration                              -->
        <!-- ================================================== -->

        <dependency>
            <groupId>com.sk89q.worldedit</groupId>
            <artifactId>worldedit-bukkit</artifactId>
            <version>7.3.0</version>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>


        <!-- ================================================== -->
        <!-- WorldGuard API                                    -->
        <!-- Optional integration                              -->
        <!-- ================================================== -->

        <dependency>
            <groupId>com.sk89q.worldguard</groupId>
            <artifactId>worldguard-bukkit</artifactId>
            <version>7.0.13</version>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>


        <!-- ================================================== -->
        <!-- PvPManager API                                    -->
        <!-- Optional integration                              -->
        <!-- ================================================== -->

        <dependency>
            <groupId>com.rylinaux</groupId>
            <artifactId>PvPManager</artifactId>
            <version>3.27.1</version>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>

    </dependencies>


    <!-- ====================================================== -->
    <!-- BUILD                                                  -->
    <!-- ====================================================== -->

    <build>

        <finalName>CombatKeepInventory-1.1.0-SNAPSHOT-build1</finalName>

        <plugins>

            <!-- Java compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>

                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>


            <!-- Create normal plugin JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>

                <configuration>
                    <archive>
                        <manifest>
                            <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

        </plugins>

    </build>

</project>